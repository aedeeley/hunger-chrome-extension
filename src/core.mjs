import "./catalog.js";
import "./core.js";
export const { DEFAULTS, catalog, PROMOTIONS, normalizeDomain, normalizeSettings, isActive, getCoverage, createMatcher, buildRules } = globalThis.HungerCore;
